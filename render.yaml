services:
  - type: web
    name: agentise-mega-crm
    runtime: node
    plan: free
    region: oregon
    buildCommand: npm test
    startCommand: node server.js
    healthCheckPath: /api/health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: APP_NAME
        value: Agentise Mega CRM
